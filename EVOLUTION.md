# EVOLUTION.md — 作业 2：提示与探索模式

## 1. 你如何实现提示功能？

提示功能分两个层次实现，均位于领域层：

- **候选数提示** (`Sudoku.getCandidates(row, col)`): 枚举给定单元格中所有合法的数字（1–9），即填入后不会引起行、列或 3×3 宫格冲突的数字。它复用了已有的 `hasConflict()` 方法。
- **下一步提示** (`Sudoku.findHint()`): 扫描整个棋盘，寻找候选数最少的空单元格。如果某个单元格只有一个候选数（唯一余数），则立即返回；否则返回候选数最少（≥ 2）的单元格，暗示用户可能需要进入探索模式。

这两种方法都是对数独棋盘状态的纯计算，不修改棋盘也不依赖外部状态。`Game` 通过薄代理方法（`getCandidates`, `getHint`）公开这些接口，使得 UI 层可以通过 `currentGame` 引用调用它们，而无需了解 `Sudoku` 的内部实现。

---

## 2. 你认为提示功能更属于 Sudoku 还是 Game？为什么？

提示功能主要属于 **Sudoku**，而 **Game** 则作为公共 API 门面。

| 职责 | 归属 | 原因 |
|---|---|---|
| 候选数枚举 | Sudoku | 基于 9×9 网格的纯计算，不需要会话状态 |
| 寻找最佳提示位置 | Sudoku | 同上，需要扫描棋盘并计算候选数 |
| 向 UI 层公开接口 | Game | 存储层持有 `currentGame` 对象，通过 `Game` 转发请求更符合现有的架构模式 |

这种划分延续了 Homework 1 的设计模式：`Sudoku` 负责棋盘逻辑（填数、验证、克隆），而 `Game` 负责会话逻辑（撤销/重做、探索状态）。

---

## 3. 你如何实现探索模式？

探索模式被实现为 `Game` 对象上的一个 **子会话状态**。它没有引入新的类，而是通过扩展 `Game` 的属性和方法来实现。

### 核心状态
- `isExploring`: 标识是否处于探索模式。
- `exploreCheckpoint`: 记录进入探索模式时的快照（包含棋盘镜像和历史记录长度）。
- `deadEndKeys`: 存储已确认失败的局面特征（序列化后的字符串），用于“记忆”功能。

### 核心方法
- `startExplore()`: 保存当前局面快照和历史记录位置，开启探索模式。
- `commitExplore()`: 退出探索模式，保留所有探索期间的移动。
- `abandonExplore()`: 若当前局面存在冲突，记录该局面为“死胡同”；然后将棋盘和历史记录回滚到快照点。
- `checkDeadEnd()`: 检查当前局面是否曾被标记为“死胡同”。

---

## 4. 主局面与探索局面的关系是什么？

- **共享对象**: 探索模式直接在当前的 `Sudoku` 实例上运行。
- **快照回滚**: 进入探索模式时，通过 `toJSON()` 对 `Sudoku` 进行深拷贝并保存。如果选择放弃探索，则从快照中恢复。
- **合并方式**: 提交探索结果时，只需清除探索标志，当前的 `Sudoku` 实例自然就成为了主局面。
- **死胡同记忆**: 使用 `JSON.stringify(toJSON())` 作为键值，在 `deadEndKeys` 集合中持久化存储（在该游戏实例生命周期内）。

这种设计避免了维护两个并行的数独实例，降低了内存开销，同时简化了撤销/重做的逻辑。

---

## 5. 你的 history 结构在本次作业中是否发生了变化？

核心的历史记录结构（`past` 和 `future` 线性栈）**保持不变**，没有引入树状或 DAG 结构。

演进是 **增量式** 的：
- **回滚机制**: 通过 `exploreCheckpoint` 记录的 `pastSnapshot`。在放弃探索时，直接将 `past` 恢复到该快照，从而实现快速回退到探索起点。
- **撤销/重做**: 在探索过程中，用户依然可以使用正常的 Undo/Redo，这些操作会正常推入/弹出 `past` 栈。

---

## 6. Homework 1 中的哪些设计，在 Homework 2 中暴露出了局限？

- **`Game.guess()` 缺乏明确的反馈信号**: 在 HW1 中，`guess()` 遇到非法位置会直接抛出异常，而 UI 层通常会捕获并忽略。这使得在探索模式下，区分“非法单元格”和“导致冲突的探索”变得稍微复杂。
- **缺乏“会话状态”概念**: HW1 的 `Game` 纯粹是历史记录的包装器。在 HW2 中添加 `isExploring` 等属性虽然简单，但如果状态变得更复杂，可能需要引入更正式的状态机模式。
- **线性历史栈的局限**: 虽然可以通过截断模拟回滚，但如果用户想要保留多条探索路径，线性栈就无法满足需求。

---

## 7. 如果重做一次 Homework 1，你会如何修改原设计？

1. **更早地引入 `validate()` API**: HW1 的冲突检测主要在 UI 适配层。将其下沉到 `Sudoku.validate()` 可以使领域对象更自洽。
2. **解耦填数与冲突检测**: 允许输入冲突值，但通过 `validate()` 标记错误。这在 HW2 的基线代码中已得到修正，效果很好。
3. **在 `Game` 中预留 `state` 属性**: 即使 HW1 只有一种状态，预留 `state: 'normal'` 会让 HW2 的演进更自然。
4. **统一坐标体系**: 强制在所有层级使用 `{row, col}` 对象，避免在字符串坐标和数字坐标之间反复转换。

---

